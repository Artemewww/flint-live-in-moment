# 🗄️ Применение миграций БД — ИНСТРУКЦИЯ

## ✅ Что сделать СЕЙЧАС

### 1. Открыть Supabase Dashboard
Перейти: https://supabase.com/dashboard → выбрать проект Flint

### 2. SQL Editor
Меню слева → **SQL Editor** → **New query**

### 3. Применить миграции ПО ПОРЯДКУ

Скопировать и выполнить каждую миграцию:

#### 1️⃣ Техника безопасности
```sql
-- Содержимое: supabase/migrations/2026-safety-briefing.sql
-- Скопировать весь файл и выполнить
```

#### 2️⃣ Коллективные закупки
```sql
-- Содержимое: supabase/migrations/2026-group-shopping.sql
-- Скопировать весь файл и выполнить
```

#### 3️⃣ Гости и координаты встречи
```sql
-- Содержимое: supabase/migrations/2026-guests-pickup.sql
-- Скопировать весь файл и выполнить
```

#### 4️⃣ Снаряжение участников
```sql
-- Содержимое: supabase/migrations/2026-equipment-ai.sql
-- Скопировать весь файл и выполнить
```

#### 5️⃣ ИИ-менеджер групповых чатов

```sql
-- Содержимое: supabase/migrations/2026-group-ai-manager.sql
-- Скопировать весь файл и выполнить
```

#### 6️⃣ Картинка и подпись сбора (fundraisers.image_url / image_caption)

```sql
-- Содержимое: supabase/migrations/2026-09-15-fundraisers-image.sql
-- Скопировать весь файл и выполнить (две команды ALTER TABLE ... ADD COLUMN IF NOT EXISTS)
```
После применения — повторно запустить сидер: `npx tsx scripts/seed-bison-manishki.ts`
(он дозаполнит `image_url` и `image_caption` у сбора «Манишки FLINT»).

#### 7️⃣ Бади-пары (event_buddies) — ОБЯЗАТЕЛЬНО

```sql
-- Содержимое: supabase/migrations/2026-09-25-buddy-pairs.sql
-- Скопировать весь файл и выполнить
```
Без этой таблицы кнопка «🤝 Бади» в боте и блок «Твой бади» в карточке события
работать не будут (код это переживёт — просто не покажет пару).
✅ Применена 26.09.2026.

#### 8️⃣ Анкета участника: пол, дата рождения, телефон — ОБЯЗАТЕЛЬНО

```sql
-- Содержимое: supabase/migrations/2026-member-gender.sql
ALTER TABLE members ADD COLUMN IF NOT EXISTS gender text;
-- Страховка: код пишет в эти колонки, отдельной миграции на них нет
ALTER TABLE members ADD COLUMN IF NOT EXISTS birthday date;
ALTER TABLE members ADD COLUMN IF NOT EXISTS phone text;
```
Без `gender` анкета в боте не сохранит пол (бот скажет об ошибке).
✅ Применена 26.09.2026.

---

## ⚙️ Проверка

После применения всех миграций проверить в **Table Editor**:

✅ Новые таблицы появились:
- `safety_briefings` — правила безопасности
- `safety_confirmations` — подтверждения участников
- `group_shopping` — коллективные закупки
- `shopping_participants` — участники закупок
- `food_categories` — категории еды (заполнена)
- `pickup_points` — точки забора гостей
- `auto_reminders` — автоматические напоминания
- `member_equipment` — снаряжение участников
- `group_messages` — история групповых чатов
- `bot_group_actions` — действия бота в группах
- `ai_learning_patterns` — паттерны обучения ИИ
- `event_groups` — привязка групп к событиям
- `event_buddies` — бади-связки события (кто за кого отвечает)

✅ Новые колонки в `members`: `gender`, `birthday`, `phone`

✅ Новые функции (Functions):
- `get_unconfirmed_safety` — кто не подтвердил техбезопасность
- `create_reminder` — создание напоминания
- `get_chat_context` — получить историю чата
- `update_shopping_list` — пересчёт списка закупок

---

## 🚀 Vercel — автоматический деплой

Код уже запушен → Vercel автоматически задеплоит через 2-3 минуты.

Проверить: https://vercel.com/dashboard → выбрать проект → **Deployments**

---

## 📝 После деплоя

1. **Настроить Vercel Cron** (если ещё не настроен)
   - Settings → Cron Jobs → Add
   - Path: `/api/cron/auto-reminders`
   - Schedule: `*/15 * * * *` (каждые 15 минут)

2. **Проверить ENV переменные** в Vercel:
   - `GEMINI_API_KEY` или `GOOGLE_API_KEY`
   - `CRON_SECRET`
   - `TELEGRAM_BOT_TOKEN`
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`

3. **Протестировать** новые функции:
   - Добавить правила безопасности для локации
   - Запустить коллективную закупку
   - Подключить группу к событию: `/link_event <event_id>`

---

## 🎯 Готово!

После применения миграций и деплоя все новые функции заработают автоматически! 🚀
