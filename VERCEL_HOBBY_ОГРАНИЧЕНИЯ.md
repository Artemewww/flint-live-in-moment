# ⚠️ Ограничения Vercel Hobby плана

## Текущая ситуация

**Vercel Hobby план позволяет:**
- ✅ Максимум 12 serverless функций
- ✅ Cron-задачи только **1 раз в сутки** (`0 6 * * *` и подобные)
- ❌ Частые крон (каждые 15 минут) — **НЕ РАБОТАЮТ**

**Сейчас настроено:**
- Cron: `0 6 * * *` (каждый день в 6:00 UTC = 9:00 МСК)
- Выполняет: все напоминания (суточные + автонапоминания за 24ч)

---

## 🚫 Что НЕ работает на Hobby

### Автонапоминания каждые 15 минут

**Функции, требующие частого крона:**
- Техника безопасности (напоминание каждый час до подтверждения)
- Координаты гостей (пинг каждые 2 часа)
- Согласование закупки (напоминание неответившим)

**Текущее поведение:**
- Напоминания отправляются **1 раз в день** (утром в 9:00 МСК)
- Если человек не ответил утром — следующее напоминание только завтра

---

## ✅ Что РАБОТАЕТ на Hobby

### Суточные напоминания (всё ОК!)
- ✅ За 7/3/1 день до события
- ✅ Отзывы на следующий день после события
- ✅ Автозакрытие голосований по дедлайну
- ✅ Подогрев (социальное доказательство при приросте участников)
- ✅ Чистка галерей через 7 дней

### ИИ-функции (всё ОК!)
- ✅ ИИ-консьерж (отвечает на вопросы)
- ✅ Парсинг снаряжения свободным текстом
- ✅ Умная постановка задач с тегами
- ✅ ИИ-менеджер групп (обучается на переписке)

---

## 🔧 Решения

### Вариант 1: Upgrade на Vercel Pro ($20/месяц)

**Преимущества:**
- ✅ Cron каждые 15 минут (до 1 минуты)
- ✅ Больше serverless функций (100 вместо 12)
- ✅ Больше bandwidth и executions

**Как апгрейднуть:**
1. Vercel Dashboard → Settings → Billing
2. Upgrade to Pro
3. Измени `vercel.json`:
   ```json
   "schedule": "*/15 * * * *"
   ```
4. Push в GitHub → автодеплой

---

### Вариант 2: Внешний cron (бесплатно, но сложнее)

**Используй cron-job.org или сервер:**

```bash
# Добавь на cron-job.org (или свой сервер)
*/15 * * * * curl -X POST https://your-domain.vercel.app/api/cron/reminders \
  -H "Authorization: Bearer $CRON_SECRET"
```

**Настройка:**
1. Зарегистрируйся на https://cron-job.org
2. Создай задание:
   - URL: `https://your-domain.vercel.app/api/cron/reminders`
   - Schedule: `*/15 * * * *`
   - Header: `Authorization: Bearer YOUR_CRON_SECRET`
3. В Vercel Environment Variables добавь `CRON_SECRET` (любая строка)

**Минусы:**
- Зависимость от внешнего сервиса
- Нужно руками настраивать headers

---

### Вариант 3: Оставить как есть (суточный крон)

**Подходит, если:**
- Участники дисциплинированы (отвечают с первого раза)
- Не критично, если напоминание придёт завтра
- Основной фокус на события (за 7/3/1 день) — это работает отлично

**Текущая настройка:**
- `vercel.json`: `"schedule": "0 6 * * *"`
- Крон выполняется в 9:00 МСК каждый день

---

## 📊 Рекомендация

**Для продакшена с активным клубом → Vercel Pro**
- Автонапоминания критичны для UX
- $20/месяц окупаются удобством участников

**Для тестирования/MVP → Hobby + внешний cron**
- Бесплатно
- Работает, но требует настройки

**Для небольшого клуба (до 20 человек) → Hobby без изменений**
- Суточных напоминаний достаточно
- Все ИИ-функции работают

---

## 🚀 Следующие шаги

1. **Выбери вариант** (Pro / внешний cron / оставить как есть)
2. **Если Pro**: измени `vercel.json` → `"schedule": "*/15 * * * *"` → push
3. **Если внешний cron**: настрой cron-job.org + добавь `CRON_SECRET`
4. **Если оставить**: ничего не делай — всё уже работает!

Текущий деплой пройдёт успешно с `0 6 * * *` — это валидный schedule для Hobby.
